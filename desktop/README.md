# Desktop build dependencies

## Electron (installer)

Установите зависимости Electron:

```bash
cd desktop
npm install
```

Сборка инсталлятора:

```bash
npm run dist
```

## Backend (PyInstaller)

См. `desktop/resources/server/README.md` для зависимостей Python и команды сборки.
