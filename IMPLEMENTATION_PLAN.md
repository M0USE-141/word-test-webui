# Комплексный план выполнения задач TestMasterBSU

## Созданные Git-ветки

Все ветки созданы от `main`:

1. `feature/user-authentication` - Регистрация и авторизация
2. `feature/user-profile` - Профиль пользователя
3. `feature/access-control` - Категории доступа к коллекциям
4. `feature/test-editing` - Редактирование тестов
5. `feature/test-collections-ui` - UI для разных типов коллекций
6. `feature/owner-statistics` - Статистика для владельцев
7. `feature/statistics-filters` - Фильтры статистики
8. `feature/attempt-statistics` - Расширенная статистика попыток
9. `feature/collection-badges` - Пометки коллекций
10. `refactor/code-modularization` - Рефакторинг и модularизация кода
11. `feature/improvements-suggestions` - Предложения по улучшению

---

## Порядок выполнения (по приоритету и зависимостям)

### ФАЗА 1: Фундамент системы пользователей (Недели 1-3)

#### 1. Задача #10: Рефакторинг кода
**Ветка:** `refactor/code-modularization`
**Приоритет:** КРИТИЧЕСКИЙ (выполнить первым)
**Зависимости:** Нет

**Обоснование:** Перед добавлением новых функций необходимо реорганизовать код для улучшения поддерживаемости.

**Что нужно сделать:**
- Разделить `api.py` (1334 строки) на модули:
  - `api/routes/tests.py` - эндпоинты тестов
  - `api/routes/attempts.py` - эндпоинты попыток
  - `api/routes/statistics.py` - эндпоинты статистики
  - `api/routes/assets.py` - работа с ассетами
  - `api/routes/auth.py` - будущая авторизация
- Разделить `app.js` (1118 строк) на модули:
  - `js/screens/management.js` - экран управления
  - `js/screens/testing.js` - экран тестирования
  - `js/screens/statistics.js` - экран статистики
  - `js/components/modal.js` - модальные окна
  - `js/utils/helpers.js` - утилиты
- Создать `api/database.py` для будущей работы с БД
- Создать `api/models/` для моделей данных
- Создать структуру для middleware и dependencies

**Результат:** Чистая модульная структура кода, готовая для расширения.

---

#### 2. Задача #1: Регистрация и авторизация пользователей
**Ветка:** `feature/user-authentication`
**Приоритет:** КРИТИЧЕСКИЙ
**Зависимости:** Рефакторинг кода

**Что нужно сделать:**

**Backend:**
- Добавить SQLite/PostgreSQL БД (выбор через переменную окружения)
- Создать модель `User`:
  ```python
  id: UUID
  username: str (unique)
  email: str (unique)
  password_hash: str
  created_at: datetime
  last_login: datetime
  is_active: bool
  ```
- Создать модель `Session`:
  ```python
  id: UUID
  user_id: UUID (FK)
  token: str
  created_at: datetime
  expires_at: datetime
  last_activity: datetime
  ```
- Реализовать эндпоинты:
  - `POST /api/auth/register` - регистрация
  - `POST /api/auth/login` - вход (возвращает JWT token)
  - `POST /api/auth/logout` - выход
  - `GET /api/auth/me` - текущий пользователь
  - `POST /api/auth/refresh` - обновление токена
- Добавить middleware для проверки токена
- Реализовать логику сессий:
  - Таймаут: 1 час без активности
  - Продление при любой активности пользователя
  - Автоматическая очистка истекших сессий

**Frontend:**
- Создать экран авторизации/регистрации (показывать при отсутствии активной сессии)
- Добавить формы:
  - Регистрация (username, email, password, password confirmation)
  - Вход (email/username, password)
- Сохранять JWT в localStorage
- Добавить проверку токена при загрузке приложения
- Реализовать автоматическое обновление токена
- Перехватывать 401 ошибки и редиректить на логин

**Результат:** Полноценная система аутентификации с сессиями.

---

#### 3. Задача #2: Профиль пользователя
**Ветка:** `feature/user-profile`
**Приоритет:** ВЫСОКИЙ
**Зависимости:** Авторизация

**Что нужно сделать:**

**Backend:**
- Расширить модель `User`:
  ```python
  display_name: str (nullable)
  avatar_path: str (nullable)
  avatar_size: int (bytes)
  ```
- Создать эндпоинты:
  - `GET /api/users/me/profile` - получить профиль
  - `PATCH /api/users/me/profile` - обновить имя
  - `POST /api/users/me/avatar` - загрузить аватар
  - `DELETE /api/users/me/avatar` - удалить аватар
- Реализовать обработку аватарок:
  - Ограничение размера: 2 МБ
  - Сжатие до максимум 512x512px
  - Поддержка форматов: PNG, JPG, GIF
  - Сохранение в `data/avatars/{user_id}.{ext}`
- Переместить статистику в профиль (подготовка к следующим задачам)

**Frontend:**
- Создать экран профиля
- Добавить компоненты:
  - Редактирование отображаемого имени
  - Загрузка/изменение аватарки (превью, кроп, сжатие)
  - Отображение статистики (текущий clientId → userId)
  - Управление коллекциями тестов (перенос из главного экрана)
- Добавить кнопку профиля в навигацию

**Результат:** Полноценный профиль пользователя с настройками.

---

### ФАЗА 2: Система доступа и совместной работы (Недели 4-6)

#### 4. Задача #3: Категории доступа к коллекциям
**Ветка:** `feature/access-control`
**Приоритет:** КРИТИЧЕСКИЙ
**Зависимости:** Авторизация, Профиль

**Что нужно сделать:**

**Backend:**
- Создать enum `AccessLevel`:
  ```python
  PRIVATE = "private"     # Только владелец
  SHARED = "shared"       # Владелец + указанные пользователи
  PUBLIC = "public"       # Все пользователи
  ```
- Обновить структуру теста (добавить в meta.json):
  ```python
  owner_id: UUID
  access_level: AccessLevel
  created_at: datetime
  updated_at: datetime
  ```
- Создать модель `TestShare`:
  ```python
  id: UUID
  test_id: UUID (FK)
  user_id: UUID (FK)
  permission: enum (viewer, collaborator)
  shared_at: datetime
  shared_by: UUID (FK)
  ```
- Создать эндпоинты:
  - `PATCH /api/tests/{test_id}/access` - изменить уровень доступа
  - `POST /api/tests/{test_id}/shares` - добавить пользователя (для shared)
  - `DELETE /api/tests/{test_id}/shares/{user_id}` - удалить доступ
  - `GET /api/tests/{test_id}/shares` - список пользователей с доступом
- Добавить проверки доступа во все эндпоинты тестов:
  - Чтение: owner OR shared user OR public
  - Редактирование: только owner
  - Удаление: только owner
- Ограничения для public/shared тестов:
  - Запретить изменение критичных настроек (количество вопросов фиксировано)
  - Только owner может редактировать вопросы

**Frontend:**
- Добавить выбор уровня доступа при создании теста
- Добавить настройки доступа в редактор теста:
  - Выбор уровня доступа
  - Для shared: UI добавления/удаления пользователей (поиск по username/email)
- Показывать иконки доступа на карточках тестов
- Блокировать редактирование для не-владельцев

**Результат:** Полноценная система управления доступом к тестам.

---

#### 5. Задача #4: Редактирование теста
**Ветка:** `feature/test-editing`
**Приоритет:** ВЫСОКИЙ
**Зависимости:** Система доступа

**Что нужно сделать:**

**Backend:**
- Создать модель `ChangeRequest` (для предложений изменений от не-владельцев):
  ```python
  id: UUID
  test_id: UUID (FK)
  user_id: UUID (FK)
  type: enum (add_question, edit_question, delete_question, edit_settings)
  payload: JSON (предлагаемые изменения)
  status: enum (pending, approved, rejected)
  created_at: datetime
  reviewed_at: datetime
  reviewed_by: UUID (FK, nullable)
  ```
- Создать эндпоинты:
  - `POST /api/tests/{test_id}/change-requests` - создать предложение
  - `GET /api/tests/{test_id}/change-requests` - список предложений
  - `POST /api/tests/{test_id}/change-requests/{req_id}/approve` - одобрить
  - `POST /api/tests/{test_id}/change-requests/{req_id}/reject` - отклонить
- Исправить сохранение вопросов с объектами (формулы, изображения):
  - Добавить валидацию ContentItem
  - Проверить сериализацию/десериализацию
  - Добавить тесты для краевых случаев
- Расширить API редактирования:
  - `PATCH /api/tests/{test_id}` - переименование, изменение доступа
  - Проверка прав: только owner

**Frontend:**
- Улучшить редактор вопросов:
  - Исправить баг сохранения объектов (формулы, изображения)
  - Добавить валидацию перед отправкой
  - Показывать превью объектов
- Для не-владельцев:
  - Заменить кнопки редактирования на "Предложить изменение"
  - Создать UI для отправки предложений
  - Показывать статус предложений
- Для владельцев:
  - Добавить список предложений изменений
  - UI для просмотра, одобрения, отклонения
  - Уведомления о новых предложениях

**Результат:** Полноценная система редактирования с поддержкой предложений.

---

### ФАЗА 3: Расширенная статистика и UI (Недели 7-9)

#### 6. Задача #5: Отдельное окно для приватных/публичных/общих коллекций
**Ветка:** `feature/test-collections-ui`
**Приоритет:** СРЕДНИЙ
**Зависимости:** Система доступа

**Что нужно сделать:**

**Backend:**
- Обновить `GET /api/tests`:
  - Добавить параметры фильтрации: `access_level`, `owner`, `shared_with_me`
  - Добавить пагинацию: `limit`, `offset`
  - Сортировка по дате создания/обновления
- Создать эндпоинт `GET /api/tests/discover`:
  - Только публичные тесты
  - Пагинация
  - Поиск по названию

**Frontend:**
- Создать табы/разделы:
  - "Мои тесты" (приватные, владельцем является текущий пользователь)
  - "Общие со мной" (shared тесты, где есть доступ)
  - "Публичные" (все публичные тесты)
- Реализовать ленивую загрузку (infinite scroll):
  - Первая загрузка: 20 тестов
  - Подгрузка при скролле: +20 тестов
  - Индикатор загрузки
- Добавить поиск и фильтры в каждом разделе

**Результат:** Удобная навигация по коллекциям с разделением по доступу.

---

#### 7. Задача #9: Пометка коллекций
**Ветка:** `feature/collection-badges`
**Приоритет:** НИЗКИЙ
**Зависимости:** UI коллекций

**Что нужно сделать:**

**Backend:**
- Добавить флаги в ответ `GET /api/tests`:
  ```json
  {
    "id": "...",
    "isOwner": true/false,
    "isShared": true/false,
    "isPublic": true/false
  }
  ```

**Frontend:**
- Добавить бейджи/иконки на карточки тестов:
  - В "Моих тестах": показать, если тест shared/public
  - В "Общих со мной" и "Публичных": показать иконку владельца, если owner = текущий пользователь
- Легенда для обозначений

**Результат:** Визуальные индикаторы статуса коллекций.

---

#### 8. Задача #8: Статистика попыток
**Ветка:** `feature/attempt-statistics`
**Приоритет:** СРЕДНИЙ
**Зависимости:** Профиль

**Что нужно сделать:**

**Backend:**
- Обновить расчет статистики попыток:
  - Использовать `questionCount` из настроек попытки для процентных соотношений
  - Пересчитать:
    - `percentCorrect = (correct / questionCount) * 100`
    - `accuracy = (correct / answered) * 100`
    - `answerRate = (answered / questionCount) * 100`
- Добавить в `stats.json`:
  ```json
  {
    "questionCountUsed": 20,
    "correctAnswers": 18,
    "incorrectAnswers": 2,
    "unansweredQuestions": 0,
    "percentCorrect": 90.0,
    "percentIncorrect": 10.0,
    "percentUnanswered": 0.0
  }
  ```
- Создать endpoint для пересчета всех старых статистик:
  - `POST /api/stats/recalculate` - пересчитать все попытки

**Frontend:**
- Обновить отображение статистики попыток:
  - Показывать процент от указанного количества вопросов
  - Диаграммы: верные/неверные/неотвеченные
- Добавить легенду с пояснениями расчетов

**Результат:** Точная статистика с использованием настроек попытки.

---

#### 9. Задача #6: Статистика для владельцев
**Ветка:** `feature/owner-statistics`
**Приоритет:** ВЫСОКИЙ
**Зависимости:** Система доступа, Статистика попыток

**Что нужно сделать:**

**Backend:**
- Обновить модель попытки (связать с пользователем):
  ```python
  user_id: UUID (FK)  # заменяет clientId
  ```
- Миграция: перенести существующие clientId в анонимных пользователей
- Создать эндпоинт:
  - `GET /api/tests/{test_id}/statistics` - статистика по тесту для owner:
    - Общее количество попыток
    - Список пользователей
    - Средняя точность по всем пользователям
    - Топ пользователей по точности
    - График попыток по времени
- Проверка доступа: только owner теста

**Frontend:**
- Создать раздел "Статистика теста" (доступен только owner):
  - Общая статистика
  - Список пользователей с их результатами
  - Фильтрация по пользователям
  - Графики и диаграммы
- Кнопка доступа в карточке теста (только для owner)

**Результат:** Владельцы могут видеть статистику всех пользователей своих тестов.

---

#### 10. Задача #7: Фильтры статистики
**Ветка:** `feature/statistics-filters`
**Приоритет:** ВЫСОКИЙ
**Зависимости:** Статистика для владельцев

**Что нужно сделать:**

**Backend:**
- Обновить `GET /api/stats/attempts`:
  - Параметры фильтрации:
    ```
    access_level: private | shared | public | all
    test_id: UUID | all
    start_date: ISO8601
    end_date: ISO8601
    attempt_id: UUID | all
    user_id: UUID | me | all (только для owner тестов)
    ```
  - Пагинация
  - Сортировка
- Создать `GET /api/stats/summary`:
  - Агрегированная статистика по фильтрам
  - Средние показатели
  - Графики временных рядов

**Frontend:**
- Создать панель фильтров в статистике:
  - Категория доступа: приватные/общие/публичные/все
  - Выбор теста: все/конкретный тест (список тестов в категории)
  - Диапазон дат: по умолчанию последний месяц, пикер дат
  - Выбор попытки: все/конкретная
  - Участники (только для owner):
    - Я (всегда по умолчанию для приватных)
    - Все (для shared/public)
    - Конкретный пользователь (список)
- Применение фильтров:
  - Обновление графиков и таблиц
  - Сохранение состояния фильтров в URL/localStorage

**Результат:** Мощная система фильтрации статистики.

---

### ФАЗА 4: Улучшения и предложения (Неделя 10+)

#### 11. Задача #11: Предложения по улучшению
**Ветка:** `feature/improvements-suggestions`
**Приоритет:** НИЗКИЙ
**Зависимости:** Все предыдущие задачи

**Что нужно проанализировать и предложить:**

**Технические улучшения:**
1. Добавить кеширование (Redis):
   - Кеш тестов (избежать повторного чтения JSON)
   - Кеш статистики
   - Кеш сессий
2. Добавить WebSocket для реал-тайм обновлений:
   - Уведомления о новых предложениях изменений
   - Live статистика
   - Коллаборативное редактирование
3. Добавить полнотекстовый поиск:
   - Поиск по содержимому вопросов
   - Поиск по названиям тестов
4. Улучшить безопасность:
   - Rate limiting для API
   - CSRF защита
   - XSS защита
   - Валидация всех входных данных
5. Мониторинг и логирование:
   - Sentry для отслеживания ошибок
   - Prometheus + Grafana для метрик
   - Структурированное логирование

**Функциональные улучшения:**
1. Экспорт/импорт тестов:
   - JSON формат
   - CSV для статистики
   - PDF отчеты
2. Теги и категории тестов:
   - Организация по темам
   - Фильтрация по тегам
3. Таймеры для тестов:
   - Ограничение времени на тест
   - Ограничение времени на вопрос
4. Комментарии к вопросам:
   - Пользователи могут оставлять комментарии
   - Обсуждение сложных вопросов
5. Геймификация:
   - Достижения
   - Рейтинги пользователей
   - Стрики (серии правильных ответов)
6. Мобильное приложение:
   - PWA версия
   - Native приложения (Flutter/React Native)

**UI/UX улучшения:**
1. Адаптивный дизайн:
   - Поддержка мобильных устройств
   - Планшеты
2. Accessibility:
   - ARIA labels
   - Поддержка клавиатуры
   - Высокий контраст
3. Анимации и переходы:
   - Плавные переходы между экранами
   - Микроинтеракции
4. Офлайн режим:
   - Service Worker
   - Кеширование тестов для прохождения офлайн

**Результат:** Список приоритизированных улучшений с оценкой сложности.

---

## Матрица зависимостей

```
Рефакторинг (10)
    ↓
Авторизация (1)
    ↓
Профиль (2)
    ↓
Система доступа (3) ──┬──→ UI коллекций (5) ──→ Пометки (9)
    ↓                 │
Редактирование (4)    │
                      ↓
            Статистика попыток (8)
                      ↓
            Статистика владельцев (6)
                      ↓
            Фильтры статистики (7)
                      ↓
            Улучшения (11)
```

---

## Рекомендуемая последовательность мердж-реквестов

1. `refactor/code-modularization` → `main`
2. `feature/user-authentication` → `main`
3. `feature/user-profile` → `main`
4. `feature/access-control` → `main`
5. `feature/test-editing` → `main`
6. `feature/test-collections-ui` → `main`
7. `feature/attempt-statistics` → `main`
8. `feature/owner-statistics` → `main`
9. `feature/statistics-filters` → `main`
10. `feature/collection-badges` → `main`
11. `feature/improvements-suggestions` → `main` (документация)

---

## Критические моменты

### Миграция данных
При внедрении системы пользователей необходимо:
1. Создать "анонимных" пользователей для существующих clientId
2. Связать существующие попытки с анонимными пользователями
3. Связать существующие тесты с первым зарегистрированным пользователем (admin)
4. Создать скрипт миграции: `scripts/migrate_to_user_system.py`

### Обратная совместимость
- Сохранить возможность использования старых тестов
- API должен поддерживать legacy clientId для статистики
- Постепенный переход на новую модель данных

### Тестирование
Для каждой фазы:
- Unit тесты для новых моделей и API
- Integration тесты для эндпоинтов
- E2E тесты для критичных сценариев (регистрация, создание теста, прохождение теста)

### Документация
После каждой фазы обновлять:
- API документацию (OpenAPI/Swagger)
- README с инструкциями по развертыванию
- CHANGELOG с описанием изменений

---

## Оценка сложности (Story Points)

| Задача | Сложность | Время (дни) |
|--------|-----------|-------------|
| Рефакторинг | 13 | 3-5 |
| Авторизация | 13 | 5-7 |
| Профиль | 8 | 3-4 |
| Система доступа | 13 | 5-7 |
| Редактирование | 8 | 3-4 |
| UI коллекций | 5 | 2-3 |
| Статистика попыток | 5 | 2-3 |
| Статистика владельцев | 8 | 3-4 |
| Фильтры статистики | 8 | 3-4 |
| Пометки | 3 | 1-2 |
| Улучшения | 5 | 2-3 |

**Итого:** ~90 SP ≈ 34-46 рабочих дней (7-9 недель при полной занятости)

---

## Команды для начала работы

```bash
# Начать работу над задачей
git checkout feature/user-authentication
git merge main  # если нужно подтянуть изменения

# После завершения задачи
git add .
git commit -m "feat: implement user authentication system"
git push origin feature/user-authentication

# Создать Pull Request на GitHub/GitLab
# После ревью и одобрения - мердж в main
```

---

## Полезные ссылки для реализации

**Авторизация:**
- FastAPI Security: https://fastapi.tiangolo.com/tutorial/security/
- JWT: https://pyjwt.readthedocs.io/

**База данных:**
- SQLAlchemy: https://docs.sqlalchemy.org/
- Alembic (миграции): https://alembic.sqlalchemy.org/

**Frontend:**
- LocalStorage: https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage
- Fetch API: https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API

**Тестирование:**
- Pytest: https://docs.pytest.org/
- Playwright (E2E): https://playwright.dev/

---

Этот план можно корректировать по мере выполнения. Удачи в разработке! 🚀
